#!/usr/bin/perl -w

eval 'exec /usr/bin/perl -w -S $0 ${1+"$@"}'
    if 0; # not running under some shell

use strict;
use Bio::Graphics::Browser2::Render::Synteny::HTML;

Bio::Graphics::Browser2::Render::Synteny::HTML->new->run;

exit 0;

